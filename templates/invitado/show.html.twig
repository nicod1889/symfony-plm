{% extends 'base.html.twig' %}

{% block title %}Invitado{% endblock %}

{% block body %}
    <h1>Invitado</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ invitado.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ invitado.nombre }}</td>
            </tr>
            <tr>
                <th>Apellido</th>
                <td>{{ invitado.apellido }}</td>
            </tr>
            <tr>
                <th>Edad</th>
                <td>{{ invitado.edad }}</td>
            </tr>
            <tr>
                <th>Foto</th>
                <td>{{ invitado.foto }}</td>
            </tr>
            <tr>
                <th>Apodo</th>
                <td>{{ invitado.apodo }}</td>
            </tr>
            <tr>
                <th>Rubro</th>
                <td>{{ invitado.rubro }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_invitado_index') }}">back to list</a>

    <a href="{{ path('app_invitado_edit', {'id': invitado.id}) }}">edit</a>

    {{ include('invitado/_delete_form.html.twig') }}
{% endblock %}
