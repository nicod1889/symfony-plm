<form method="post" action="{{ path('app_vlog_delete', {'id': vlog.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vlog.id) }}">
    <button class="btn">Delete</button>
</form>