{% extends 'base.html.twig' %}

{% block title %}Edificio{% endblock %}

{% block body %}
    <h1>Edificio</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ edificio.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ edificio.name }}</td>
            </tr>
            <tr>
                <th>Street</th>
                <td>{{ edificio.street }}</td>
            </tr>
            <tr>
                <th>Number of street</th>
                <td>{{ edificio.numberStreet }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_edificio_index') }}">back to list</a>

    <a href="{{ path('app_edificio_edit', {'id': edificio.id}) }}">edit</a>

    {{ include('edificio/_delete_form.html.twig') }}
{% endblock %}
